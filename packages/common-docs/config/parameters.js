// 排序
export const options = {
    storySort: {
        order: [
            'common',
            'vue', 'react',
            'react-demo', 'vue-demo',
        ],
    }
};

// 文档全局配置
export const docs = {
    description: {
        // 描述内容，可以进行覆盖
        component: '所有组件都是基于antd 组件进行二次封装',
    },
};

// 视口 https://storybook.js.org/docs/vue/essentials/viewport
export const viewport = {
    disable: true,
};

// 背景 https://storybook.js.org/docs/vue/essentials/backgrounds
export const backgrounds = {
    disable: true
};

